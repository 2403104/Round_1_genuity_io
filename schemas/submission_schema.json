{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Submission Results Schema",
    "description": "Schema for participant submission JSONL files",
    "type": "object",
    "properties": {
        "query_id": {
            "type": "string",
            "description": "ID of the query being answered"
        },
        "results": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "minItems": 1,
            "maxItems": 5,
            "description": "Ordered list of retrieved file IDs (Top-5)"
        }
    },
    "required": [
        "query_id",
        "results"
    ]
}